{
  "name": "meu-projeto",
  "version": "1.0.0",
  "description": "Upload com nome e persistência",
  "main": "server.js",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "node server.js",
    "vercel-build": "npx prisma generate && npx prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "^6.8.1",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "multer": "^1.4.4"
  },
  "devDependencies": {
    "prisma": "^6.8.1"
  }
}